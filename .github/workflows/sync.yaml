name: Sync to public
on:
  push:
    tags:
      - 'v*'
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current tag
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
        with:
          ref: main
          fetch-tags: true
          fetch-depth: 0
      - name: Push to public target
        shell: bash
        run: |
          # Unset https header which contains the 'default' token for the executing repo
          git config --local --unset-all "http.https://github.com/.extraheader"
          git config --global user.email "pylon@nordicsemi.no"
          git config --global user.name "Nordic Builder"
          git remote add fork https://datgizmo:${GITHUB_TOKEN}@github.com/DatGizmo/gh-sync-target
          git rm -rf .github
          git commit -m "Strip .github folder"
          git push --force fork main
          git push fork ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.TEST_TOKEN }}
